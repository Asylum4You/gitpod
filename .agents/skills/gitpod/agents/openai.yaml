interface:
  display_name: "Gitpod"
  short_description: "Repo-specific patterns and workflows for gitpod"
  default_prompt: "Use the gitpod repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true